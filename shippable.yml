language: python

python:
	- 2.7

before_install:
  - sudo apt-get install libjpeg-dev

# install requirements 
# TODO make sure we need --use-mirrors 
# I think it's extra protection against something not downloading
install: "pip install -r requirements.txt"

# command to run tests
script: python manage.py test --settings=snap.settings.local --parallel

# Django enviornment
env: DJANGO_VERSION=1.8.3