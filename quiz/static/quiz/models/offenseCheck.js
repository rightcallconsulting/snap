var OffenseCheck = function(config){
  this.name = config.name || "";
  this.defensiveFormation = config.defensiveFormation || null;
  this.newPlay = config.newPlay || null; //play to check into if condition is satisfied
};
